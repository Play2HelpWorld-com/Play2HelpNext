
export type TokenReward = {
    userWalletAddress: string;  //the wallet address of the user
    tokenAmount: number;   //the token address, this will either be saved in the env or come from the DB
    tokenAddress: string;  //the amount the fellow is been rewarded with
  };
  
 